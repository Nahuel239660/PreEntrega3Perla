@charset "UTF-8";
/* Nuevos estilos para centrar el contenido en la sección */
.centrar-section {
  max-width: 800px;
  margin: 0 auto;
}

/* Estilos para mejorar la legibilidad y la experiencia del usuario */
p {
  line-height: 1.6;
  margin-bottom: 15px;
}

/* Estilos específicos para la página de servicio1 (Asesoramiento Informático) */
.servicio-imagen {
  width: 100%;
  max-width: 600px;
  height: auto;
  display: block;
  margin: 20px auto;
}

h1 {
  font-size: 28px;
  font-weight: bold;
  margin: 20px 0;
}

ul {
  list-style: disc;
  margin-left: 30px;
}

/* Estilos adicionales específicos para la página de Factura Electrónica y Desarrollo */
.servicio-imagen {
  width: 100%;
  max-width: 600px;
  height: auto;
  display: block;
  margin: 20px auto;
}

/* Estilos para la sección de logos de lenguajes */
.logos-lenguajes {
  display: flex;
  justify-content: space-between;
  max-width: 400px;
  margin: 20px auto;
}

.logos-lenguajes img {
  width: 60px;
  height: 60px;
  margin: 5px;
}

@media (max-width: 425px) {
  .centrar-section {
    max-width: 90%;
  }
  h1 {
    font-size: 24px;
  }
  .logos-lenguajes {
    flex-direction: column;
    align-items: center;
  }
}

/*# sourceMappingURL=css.map */
